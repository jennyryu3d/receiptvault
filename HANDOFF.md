# HANDOFF — ReceiptVault

마지막 갱신: 2026-08-27

## 이 앱이 하는 일 — 그리고 하지 않는 일

**영수증을 찍으면 세금에 쓸 수 있는 기록이 나온다.** 그게 전부다.

다루는 것:
- 지금 신고에 들어가는 것 — 사업 경비 (Schedule C)
- 나중 신고에 쓰이는 것 — 집 개량 (취득원가, 집을 팔 때)

**다루지 않는 것 (요청이 와도 넣지 않는다):**
- 세금과 무관한 지출 기록 (여행 정산, 가계부) — 다른 앱이다
- 매출·소득·판매세 — 들어온 돈은 성격이 완전히 달라서 넣는 순간 회계 프로그램이 된다
- 예산 세우기, 계획 대비 실적 — 그건 프로젝트 관리다. 리모델링 앱이 할 일
- 재고 관리, 청구서 발행

2026-08-27 에 개인 장부와 판매업 분류표를 넣었다가 **도로 뺐다.**
둘 다 "나중에 쓸지도 모른다" 였지 지금 필요한 게 아니었다.
구조는 남아 있어서 실제로 필요해지면 `categories.js` 안에서 15줄이면 돌아온다.

## 지금 상태

**배포되어 실제로 쓰고 있다.** https://jennyryu3d.github.io/receiptvault/
Supabase 스키마 적용됨, Worker(자동 인식) 배포됨, 실제 영수증 인식 성공.

| 단계 | 상태 |
|---|---|
| 앱 코드 · 배포 | 완료 (GitHub Pages) |
| Supabase 스키마 | `schema.sql` → `migrate-2.sql` → `migrate-3.sql` → `migrate-4.sql` 순서로 적용 |
| Worker (자동 인식) | 배포됨 · 본인 Anthropic 키 |
| 부부 공유 | 코드 완료 — 남편 초대는 아직 |
| 영문/한글 병기 | 완료 (v0.4.0) |
| 설정 화면 (버전·저작권·강제 갱신·연결 상태) | 완료 (v0.4.0 → 아이콘·접기 개선) |
| 장부 여러 개 + 장부 종류 | 완료 (v0.5.0) |
| 종류·업종 확장 구조 (프로필 표) | 완료 (v0.6.0) |
| 범위 정리 — 세금 관련만 남김 | 완료 (v0.7.0) |
| Resend SMTP (메일 한도) | **아직** — 지인·교회에 열기 전 필수 |
| ConvoTrans 키 교체 | **아직** — 아직 회사(KRAFTON) 키로 돌아감 |

## 다음에 할 일

1. `migrate-3.sql` → `migrate-4.sql` 실행 → 파일 업로드 → Worker 재배포 (v0.6.0)
2. 설정에서 사업체명·납세자명 넣기, 남편 초대
3. Resend SMTP 붙이기 (README 참고)
4. ConvoTrans 프록시를 본인 키로 교체

## 설계 결정과 이유

- **빌드 도구 없음** — PC가 11월까지 없다. 빌드가 끼면 코드 수정에 PC가 필요해진다.
  브라우저가 React/Babel을 직접 받는 방식이라 GitHub 웹에서 고치고 새로고침하면 끝.
  (매듭/Maedeup은 React+Vite로 잡혀 있지만 그건 PC 생긴 뒤 이야기라 다르게 간다.)
- **분류에 Schedule C 줄번호를 직접 박음** — 자기만의 분류로 모으면 연말에 전부 다시
  매핑해야 한다. 처음부터 신고서 항목으로 모은다.
- **분할은 별도 테이블이 아니라 jsonb 한 칸** — 처음엔 v1에서 빼려 했지만 혼합 영수증이
  자주 생긴다고 해서 넣었다. `receipts.splits` 에 `[{category, amount, note}]` 로 넣는다.
  별도 테이블로 만들면 조인, 두 번째 RLS 정책, 삭제 연쇄가 따라오는데 개인용 앱에서
  치를 값이 아니다. 영수증 한 건을 읽으면 분할까지 통째로 따라온다.
  - 계산 규칙은 `RV_UTIL.lines()` 한 곳에만 있다. 리포트·CSV·상세·입력 미리보기가
    전부 이걸 거친다. 새 화면을 붙일 때도 여기를 쓸 것.
  - 대표 분류(`category` 열)는 저장할 때 금액이 가장 큰 줄로 자동 설정된다.
    목록 표시와 필터가 이 값을 쓴다.
  - 사업 사용 비율은 영수증 단위다. 줄마다 다른 비율을 주면 데이터 구조와 UI가
    급격히 복잡해지는데, 그 경우는 애초에 두 건으로 나눠 넣는 게 맞다.
- **AI 인식은 선택적** — `AI_PROXY_URL`이 비어 있으면 자동 인식 버튼이 조용히 빠지고
  수동 입력만으로 앱이 완전히 동작한다. 키가 준비되면 값 하나만 채우면 붙는다.
- **이미지는 올리기 전 축소** — 긴 변 1600px / JPEG 0.72. 원본은 3~5MB라
  무료 저장소 1GB가 금방 찬다. 축소해도 영수증 글자는 읽힌다.
- **서비스워커는 network-first** — 캐시 우선으로 두면 "고쳤는데 태블릿에서 안 바뀐다"가
  생기고, 그건 태블릿에서 디버깅하기 최악인 문제다.

- **공유는 장부(ledger) 단위** — 부부가 각자 계정으로 로그인하고 장부 하나를 같이 본다.
  `ledgers` / `ledger_members` / `ledger_invites` 세 표에 RLS를 걸었다.
  - RLS 재귀를 피하려고 권한 판정을 전부 `security definer` 함수로 뺐다.
    `ledger_members` 정책 안에서 `ledger_members` 를 직접 조회하면 무한 재귀에 빠진다.
  - 초대는 이메일로 미리 만들어 두고, 그 주소로 로그인할 때 `claimInvites()` 가
    합류시킨다. 앱은 메일을 보내지 않는다 — Supabase 기본 메일이 시간당 2통이라
    초대 메일까지 거기에 얹으면 로그인 메일이 밀린다.
  - `ledgers` 의 select 정책에 `owner_id = auth.uid()` 를 따로 넣어야 한다.
    장부를 막 만든 순간에는 아직 식구 명단에 없어서, 식구 조건만 두면
    방금 만든 장부를 자기가 못 읽고 생성이 실패한다. (실제로 이 순서 버그를 잡았다.)
  - 사진 경로를 `{user_id}/…` 에서 `{ledger_id}/…` 로 바꿨다. 경로가 곧 권한이다.
- **세무사 자료는 브라우저 인쇄로 PDF를 만든다** — jsPDF 같은 라이브러리를 넣지 않았다.
  무빌드 원칙에 맞고, 용량이 0이고, 한글 폰트 임베딩 문제도 없다. 인쇄 영역은
  `.tx-paper`, 화면 전용 요소는 `.rv-noprint` 로 가린다.
  - 문서는 화면 테마와 무관하게 항상 흰 바탕 검은 글씨다 (종이에 나갈 것이므로).
  - CSV는 세무사용이라 열 이름과 분류명이 **영문**이다. 화면 리포트의 한글과 다르다.

## 검증 기록 (2026-08-25)

- 문법: `config.js`, `categories.js`, `core.js`, `sw.js`, `worker.js` — node 문법 검사 통과
- JSX: `app.jsx` 1210줄 — acorn-jsx 파싱 통과, 컴포넌트 15개 전부 정의 확인
  (이 검사로 `preview` 변수 이름 충돌을 하나 잡았다)
- 로직: 42개 테스트 통과 — 분류표 무결성, 식비 50%·사업사용비율 공제 계산,
  분할 합산·잔액 검사·경계값, **연간 집계가 Schedule C 줄번호 순서로 나오는지**,
  통화·보고서용 숫자·날짜 표기, CSV 이스케이프, 미설정 상태 감지
- 브라우저: 실제 Chromium에서 스크립트 3종 오류 0건, 분할 공제와 연간 집계가
  브라우저 안에서도 같은 값을 내는지 확인
- SQL: **실행 검증은 못 했다.** 이 환경에 PostgreSQL을 설치할 수 없었다.
  대신 구조 점검(정책 19개 전부 drop 짝 있음, 테이블 4개 전부 RLS 켜짐,
  함수 정의/호출 대응, `$$` 짝)과 손으로 훑는 검토를 했고 그 과정에서
  위의 `ledgers` select 정책 순서 버그를 잡았다. SQL Editor에서 에러가 나면
  메시지를 그대로 보내면 된다.
- **React 렌더링은 미검증** — 작업 환경에서 CDN(unpkg/cdnjs) 접근이 막혀 있어
  React/Babel을 못 받았다. 첫 배포 때 흰 화면이 나오면 이걸 먼저 의심할 것.
  브라우저 개발자 도구가 없는 태블릿이라면, `config.js` 를 채우기 전에 먼저 열어보면
  된다. 설정 안내 카드가 보이면 React까지는 정상이라는 뜻이다.

## 업데이트 2 (2026-08-27)

- **영문/한글 병기** — `labels.js` 의 `RV_T` 가 유일한 출처. `en` 은 크게, `ko` 는 작게.
  `ko` 가 없는 항목은 양쪽 나라에서 그대로 쓰는 말(Email, PDF, Schedule C)이라 영문만 나온다.
  설명문은 여기 없다 — 화면을 쓰는 사람이 읽는 글이라 한국어로 둔다.
- **통화 두 벌** — `amount_original`+`currency` 는 영수증 그대로, `total` 은 달러 환산.
  리포트·세무사 자료는 전부 `total` 기준. 환율은 거래일 기준 ECB 공시(api.frankfurter.dev,
  무료·키 불필요)를 자동 조회하고, 카드 명세서 금액을 넣으면 `fx_source='manual'` 로 그게 이긴다.
  - **분할 금액은 영수증 통화 기준**이다. 달러 환산은 `lines()` 가 환율을 곱해 만든다.
    여기를 헷갈리면 원화 영수증의 분할이 통째로 어긋난다.
- **AI 사용량 제한** — Worker 가 Supabase 토큰을 요구하고, `rv_ai_use` 함수가 한 트랜잭션
  안에서 세고 기록한다. Origin 헤더는 프로그램으로 흉내낼 수 있어 문지기가 못 된다 —
  토큰이 진짜 문지기다. 한도는 그 SQL 함수 안의 `PER_USER`, `GLOBAL` 숫자 두 개.
- **세무사 자료 보강** — 외화 구입 표(환율 출처 포함), 사업 목적 누락 경고, 식비 참석자 안내,
  그리고 **"이 앱이 다루지 않는 것"을 문서에 명시**(주행 기록부, 재고, 홈오피스).
  세무사가 없는 걸 있는 줄 알게 두는 게 제일 위험하다.
- **메모 맞춤법은 일부러 안 만들었다.** 브라우저와 삼성 키보드가 이미 하는 일이고,
  AI로 하면 메모 한 줄마다 API 호출이 붙는다. `spellCheck` 만 켜뒀다.

### 이번에 잡은 버그
- `TaxDoc` 에서 `koreanLeft` 를 선언보다 먼저 쓰고 있었다 (TDZ ReferenceError).
  선언 순서 검사를 만들어 같은 실수를 다시 못 하게 했다.

### 검증 (2026-08-27)
- 문법: 전 파일 통과. JSX 1635줄, 컴포넌트 17개 정의 확인
- 선언 순서 검사 통과 (위 버그 재발 방지)
- 로직 30개 통과 — 라벨 규칙, 원화 영수증 환산, 원화 기준 분할, 카드 청구액 역산,
  식비 50%가 환산 후에도 맞는지, 집계가 달러로 합쳐지는지
- 브라우저: 실제 Chromium 에서 오류 0건, 파일 누락 0건
- **환율 API 는 실제로 호출해 확인**: 2026-08-11 KRW→USD = 0.00071

## 구조 — 새 장부·새 업종을 늘리는 법 (v0.6.0)

세 가지를 따로 둔다. 섞으면 하나 늘릴 때마다 화면 코드를 뜯어야 한다.

| 층 | 어디 | 무엇 |
|---|---|---|
| 분류표 | `categories.js` 의 `RV_CAT_SETS` | 무엇을 샀는지 고르는 목록. 업종마다 다르다 |
| 장부 종류 | `categories.js` 의 `RV_PROFILES` | 그 돈이 세금에서 어떤 뜻인지. 화면·문서 생김새 |
| 장부 | `ledgers` 표 (`kind`, `cat_set`) | 실제 하나의 사업체·하나의 집. 몇 개든 |

**app.jsx 는 장부 종류를 하나도 모른다.** 프로필이 시키는 대로 그릴 뿐이다.
칸 이름, 표 제목, 문서 주석, CSV 열, 라벨, 입력 칸을 보일지 말지가 전부 프로필에 있다.

늘리는 법:

- **새 업종** (예: 사서 되파는 판매업) → `RV_CAT_SETS` 에 분류표 하나 추가하고,
  `business` 프로필의 `catSets` 에 이름을 넣는다. **SQL 도 화면 코드도 안 건드린다.**
  분류표가 둘 이상이 되면 장부 만들기 화면에 고르는 칸이 저절로 생긴다.
- **새 장부 종류** (예: 임대 부동산 Schedule E) → `RV_PROFILES` 에 항목 하나.
  섹션·문서 주석·CSV 열을 거기 적는다. 새 주석 문구가 필요하면 `TaxDoc` 의
  `note()` 에 case 하나를 더한다.
- **새 사업체** → 그냥 장부를 하나 더 만든다. Schedule C 는 사업체마다 따로 내는
  서류라 장부가 나뉘는 게 맞다. 같이 쓰는 사람도 장부마다 따로 정해진다.

`taxScope` 로 묶는다: `filing`(그 해 신고에 들어감) · `future`(집 팔 때).
장부 만들기 화면이 이 묶음으로 갈라서 보여준다. 세금과 무관한 장부는 두지 않는다.

**분류 key 는 앱 전체에서 유일해야 한다** (접두사 없음=가죽, `p_`=집 공사). 이미 저장된 영수증이 이 key 로 자기 분류를 찾기 때문에, 한 번 쓴 key 는
절대 바꾸지 않는다. 대신 `RV_CAT(key)` 가 모든 표를 합쳐 보므로 계산 코드
(`lines` / `deductible` / `summarize`)는 장부 종류를 몰라도 된다.

`summarize` 도 group 이름을 미리 알지 않는다. 분류표가 쓰는 group 이 곧 칸 이름이라
새 종류가 새 group 을 써도 core.js 는 그대로다.

## 장부 종류 (v0.5.0)

장부를 만들 때 종류를 고른다. 만든 뒤에는 못 바꾼다 — 분류표가 통째로 다르기 때문.

- **business** — 공방처럼 그 해에 공제받는 지출. Schedule C 줄번호로 정리된다.
- **property** — 집 리모델링처럼 공제가 아니라 집의 취득원가(cost basis)에 쌓이는 지출.
  집을 팔 때 양도차익에서 빠진다. 그래서 보고서가 "그 해 합계"가 아니라
  **연도별 누적**이고, 문서 제목도 Capital Improvement Record 다.

왜 한 앱에 넣었나: 영수증을 찍고 AI가 읽고 사진을 보관하는 부분이 완전히 같다.
다른 건 분류표와 보고서뿐이라 `kind` 하나로 갈리게 했다.

구현 방식 — **분류 key 를 전역에서 유일하게** 두고(`p_` 접두사) `RV_CAT(key)` 는
두 표를 합친 하나의 사전을 본다. 그래서 금액 계산 코드(`lines`, `deductible`,
`summarize`)는 장부 종류를 전혀 몰라도 된다. 고르는 목록만 `RV_CATS(kind)` 로 갈린다.
취득원가에 안 들어가는 분류는 `deduct: 0` 으로 둬서 같은 계산식이 그대로 쓰인다.

IRS 근거(Pub. 523): 가치를 올리거나 수명을 늘리거나 새 용도로 바꾸면 개량 → 원가에 가산.
단순 수리·유지는 아님. 단, 큰 공사의 일부로 한 수리는 개량에 포함될 수 있어서
문서에 "검토 필요" 항목으로 명시한다. 에너지 크레딧·보험금은 원가에서 빼야 하는데
앱이 추적하지 않으므로 그 사실도 문서에 적어둔다.

## 결제 수단 표기 (v0.5.0)

`receipts.payment_ref` — 'Visa ...4821' 같은 짧은 표기. 카드 명세서와 대조할 때 쓴다.
별도 표를 만들지 않았다. 이미 넣은 영수증들의 값이 곧 입력 후보 목록이다(datalist).

**카드번호 전체는 저장하지 않는다.** AI에게 끝 4자리만 달라고 시키고,
그래도 모델이 전체를 뱉을 수 있으니 `RV_UTIL.cleanPaymentRef` 가 저장 전에 한 번 더
자른다(5자리 이상 연속 숫자 → 끝 4자리만).

## 리모델링 앱과의 관계 (다음 프로젝트)

리모델링 **계획·예산·진행 관리**는 별도 앱으로 만든다. 영수증은 이 앱에 남긴다.

이유: 같은 영수증이 두 가지 일을 하는데 수명이 다르다.
- 예산 대비 실제 지출 — 공사 기간에만 필요 → 리모델링 앱
- 취득원가 증빙 — 집을 팔 때까지 10~20년 → **여기**

프로젝트 앱은 공사가 끝나면 안 쓰게 된다. 그때 증빙이 거기 들어 있으면 안 된다.

붙이는 법: 두 앱이 **같은 Supabase 프로젝트**를 쓰면 리모델링 앱이 이 앱의
`receipts` 를 읽어 "이번 달 실제 지출"을 계산할 수 있다.
리모델링 앱의 예산 항목을 `p_kitchen`, `p_bath` 같은 **이 앱의 분류 key 로 잡아두면**
예산 대비 실적이 조인 한 번으로 나온다. 사진과 AI 인식은 이 앱에만 둔다 (한도도 하나로).
